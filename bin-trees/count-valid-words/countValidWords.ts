import { BNodeStr } from "../common/bintree";

/** Count words, stopping when STOP is found. */

function countValidWords(node: BNodeStr | null): number {
  return 42;
}


export { countValidWords };
